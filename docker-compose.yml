version: '3.9'

services:
  fraud-service:
    build: ./ml_pipeline/serving
    ports:
      - "8000:8000"

  banking-api:
    build: .
    command: uvicorn banking_api.app:app --host 0.0.0.0 --port 9000
    ports:
      - "9000:9000"